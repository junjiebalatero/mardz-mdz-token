{
    "build": {
      "image": "node:alpine",
      "command": "npm install && npm run build && npm start",
      "publicDir": "build",
      "baseDir": "frontend",
      "environment": {
        "SOME_ENV": "Build finished!"
      }
    }
   }